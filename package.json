{
  "name": "gitquest-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "type-check": "pnpm --filter \"*\" type-check",
    "dev": "pnpm --filter \"*\" dev",
    "build": "pnpm --filter \"*\" build"
  },
  "dependencies": {
    "turbo": "^2.5.9"
  }
}
